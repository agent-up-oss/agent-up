# AgentUp.Sdk.Common

Shared contracts for Agent-Up capability SDKs: module identity, closed capability kinds, Nix package declarations, and registration records.

Kind SDK packages (`AgentUp.Sdk.Runtime`, `AgentUp.Sdk.Agent`) reference this library. Capability authors typically take it transitively from the kind SDK rather than referencing it directly.
