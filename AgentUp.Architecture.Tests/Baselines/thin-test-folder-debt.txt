# Required test-kind folders holding a single test: present, so the structural rule
# passes, but not actually covering the slice.
#
# Entries are removed as the tests are written; SliceTestCoverage fails on an entry that is
# already satisfied, so this file cannot drift into fiction. Additions require review.
